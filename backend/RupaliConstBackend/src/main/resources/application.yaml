spring:
  application:
    name: RupaliConstBackend

  datasource:
    url: jdbc:postgresql://localhost:5432/rupali
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jmx:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
